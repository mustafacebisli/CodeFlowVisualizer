# Ornek migration workspace

Uygulamada **Migration COBOL→Java** sekmesi → **Workspace ac** → bu klasoru secin.

Yapı: `legacy/`, `target/`, `.codeflow/golden/` — bkz. `docs/MIGRATION_PLAN.md`.

`target/` icin kendi Java Maven projenizi veya bos bir `pom.xml` ile klasor ekleyebilirsiniz.
